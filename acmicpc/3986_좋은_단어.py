# 좋은 단어의 조건
# 기본 설명 + 한 가지의 글자만 있어도 된다. AA

# 좋은 단어의 불충분 조건
# 한 글자에서 두 개가 뻗어가면 안된다. AAA
# 한 글자씩만 있으면 안 된다. AB
# 하나의 글자쌍 사이에 다른 글자가 있는데, 다른 글자가 해당 글자쌍 밖에 존재하면 안된다. ABAB

N = int(input())

for i in range(N):
    test_case = input() # 'ABAB'

    

